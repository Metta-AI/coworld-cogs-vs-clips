name: Build Coworld role images

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/build-role-images.yml"
      - "commissioner/**"
      - "reporter/**"
      - "grader/**"
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-role-images.yml"
      - "commissioner/**"
      - "reporter/**"
      - "grader/**"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: cogs-vs-clips-commissioner
            image: metta-ai/coworld-cogs-vs-clips-commissioner
            context: ./commissioner
            file: ./commissioner/commissioners/Dockerfile
            build_args: |
              COMMISSIONER_KEY=config_driven
              RULESET_STRATEGY_CONFIG_NAME=cogs_vs_clips
          - name: four-score-commissioner
            image: metta-ai/coworld-four-score-commissioner
            context: ./commissioner
            file: ./commissioner/commissioners/Dockerfile
            build_args: |
              COMMISSIONER_KEY=config_driven
              RULESET_STRATEGY_CONFIG_NAME=four_score
          - name: default-reporter
            image: metta-ai/coworld-cogs-vs-clips-default-reporter
            context: ./reporter
            file: ./reporter/default/Dockerfile
            build_args: ""
          - name: cogs-vs-clips-reporter
            image: metta-ai/coworld-cogs-vs-clips-reporter
            context: ./reporter
            file: ./reporter/cogs_vs_clips/cogs_vs_clips_summarizer/Dockerfile
            build_args: ""
          - name: grader
            image: metta-ai/coworld-cogs-vs-clips-grader
            context: ./grader/graders/cogs_v_clips/cogs_v_clips_grader
            file: ./grader/graders/cogs_v_clips/cogs_v_clips_grader/Dockerfile
            build_args: ""

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          REPO="${{ env.REGISTRY }}/${{ matrix.image }}"
          SHORT_SHA="${GITHUB_SHA::12}"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            TAGS="${REPO}:pr-${{ github.event.number }}"
          else
            TAGS="${REPO}:latest,${REPO}:main-${SHORT_SHA}"
          fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          build-args: ${{ matrix.build_args }}
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.tags.outputs.tags }}
